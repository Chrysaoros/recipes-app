import styled from "styled-components";

const StyledImageCardList = styled.section`
  display: flex;
  justify-content: center;
  align-items: space-between;
  flex-wrap: wrap;
`;

export default StyledImageCardList;
